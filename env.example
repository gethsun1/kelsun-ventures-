# ===========================================
# KelSun Ventures Portal - Environment Variables
# ===========================================

# Database Configuration
# For production, use a PostgreSQL database (recommended for Vercel)
# You can use Vercel Postgres, PlanetScale, Supabase, or any PostgreSQL provider
DATABASE_URL="postgresql://username:password@host:port/database_name"

# For development (SQLite - not recommended for production)
# DATABASE_URL="file:./dev.db"

# NextAuth.js Configuration
# Generate a secure random string for production
# You can use: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-key-here-minimum-32-characters"

# NextAuth.js URL (automatically set by Vercel in production)
# For development: http://localhost:3000
# For production: https://your-domain.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# Optional: NextAuth.js Debug Mode (set to "true" for development)
NEXTAUTH_DEBUG="false"

# ===========================================
# Production Database Recommendations
# ===========================================

# 1. Vercel Postgres (Recommended)
# - Built-in integration with Vercel
# - Automatic connection pooling
# - Easy setup through Vercel dashboard
# DATABASE_URL will be automatically provided

# 2. PlanetScale
# - Serverless MySQL platform
# - Great for scaling
# - Free tier available
# DATABASE_URL="mysql://username:password@host/database?sslaccept=strict"

# 3. Supabase
# - Open source Firebase alternative
# - PostgreSQL with real-time features
# - Free tier available
# DATABASE_URL="postgresql://postgres:password@db.project.supabase.co:5432/postgres"

# 4. Railway
# - Simple PostgreSQL hosting
# - Good for small to medium applications
# DATABASE_URL="postgresql://postgres:password@containers-us-west-xyz.railway.app:5432/railway"

# ===========================================
# Security Notes
# ===========================================

# 1. Never commit .env files to version control
# 2. Use strong, unique secrets for production
# 3. Rotate secrets regularly
# 4. Use different databases for development and production
# 5. Enable SSL/TLS for database connections in production

# ===========================================
# Vercel Deployment Notes
# ===========================================

# 1. Set these variables in Vercel Dashboard:
#    - Go to your project settings
#    - Navigate to "Environment Variables"
#    - Add each variable with appropriate values

# 2. For different environments:
#    - Production: Set for "Production" environment
#    - Preview: Set for "Preview" environment (optional)
#    - Development: Set for "Development" environment (optional)

# 3. After setting variables:
#    - Redeploy your application
#    - Variables will be available at build time and runtime
